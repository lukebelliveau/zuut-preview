import React from 'react';
import Layout from '../components/Layout';

export default function Workplace() {
  return <Layout header="Workplace">
    <p>You don't have any saved grows yet</p>
  </Layout>;
}