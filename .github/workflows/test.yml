name: test
on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 12.x

      ### figure out which testing pipeline to use
      #- name: Cypress run
      #  uses: cypress-io/github-action@v1
      #  with:
      #    build: npm run build
      #    start: npm start
      #    wait-on: http://localhost:3000
      #    browser: chrome
